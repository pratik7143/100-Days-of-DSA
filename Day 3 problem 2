int missingNum(int *arr, int size) {
int n = size + 1;

    long long total = (long long)n * (n + 1) / 2;
    long long sum = 0;

    for (int i = 0; i < size; i++) {
        sum += arr[i];
    }

    return total - sum;
